# Server only. Never use EXPO_PUBLIC_ for API keys or access tokens.
SILICONFLOW_API_KEY=
# Generate: node -e 'process.stdout.write(require("node:crypto").randomBytes(32).toString("hex"))'
VOICE_ACCESS_TOKEN=
ASR_MODEL=FunAudioLLM/SenseVoiceSmall
CLEANUP_MODEL=deepseek-ai/DeepSeek-V4-Flash
HOST=127.0.0.1
PORT=3100
ALLOWED_ORIGINS=http://localhost:8097,http://127.0.0.1:8097
