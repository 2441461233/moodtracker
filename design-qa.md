# MoodTracker · 设计与交互验收记录

## 当前 2.1.2 修订：统一 Apple 心境时间线

日期：2026-09-03。范围：将前台已读取的 Apple 心境接入今日每日列表、月历 / 年像素、来源与列表筛选，以及独立健康回顾。本次 `npm run verify` 已退出 0：TypeScript、172 项回归、Web 导出及 19 项网页测试通过，共 191 项。以下 UI 点验使用隔离合成夹具，**不是真实 HealthKit 或真机验收**；尚未测试大字体与真实 VoiceOver。2.1.2 尚未创建构建、未签名、未上传或发布。2.1.1（5）在 2026-09-03 07:30 UTC 已核实内部 TestFlight 可用的历史证据见 [发布说明](docs/ios-release.md)。

已实际点验：320px 混合日历本月 27 条、今天 24 条且分页从 20 展开至 24；本地来源本月 2 条 / 当天 1 条，Apple 来源本月 25 条，9 月 1 日 Apple 独有记录 1 条，来源关键词跨日期搜索得到 2 条。390px Apple-only 今日列表和周标记正确；Apple-only 洞察为 3 条 `dailyMood`、原始均值 +0.13，`momentaryEmotion` 显示“—”，展开为 3 条只读记录。另一个 320px Apple-only 日历夹具切换年像素后，辅助树显示“2026 年 9 月，包含 3 条 Apple 心境”，近似年均为 3.3，截图确认来源及年切换无横向溢出；未测试从像素进入月份。待读取提示、空查询权限未知及断开后移除 Apple / 保留本地的场景均通过；场景切换发生在隔离夹具中。

- 统一日期视图使用既有五档颜色和图标，Apple 行保留“Apple 健康 · 只读”、原始愉悦度、时间、来源及两种心境类型；本地日记仍可编辑，外部样本不出现本地编辑 / 删除 / 导出入口。
- 月历和年像素有 Apple 来源标记。“全部 / 本地 / Apple 健康”控制日期视图及期间回顾；关键词跨日期搜索与心情筛选仅控制列表，明确标作“列表筛选”。
- 日历的 `dailyMood` 优先、否则 `momentaryEmotion` 均值规则，以及 `valence × 2 + 3` 的 1–5 近似用途可见；期间平均日等权。Apple 独立回顾按两种类型分别汇总原始均值，本地趋势 / 分布 / 活动关联不混算。
- 仅对真实自写来源、bundle 与稳定本地 ID 精确匹配且本地仍存在的健康副本去重；孤立自写样本、外部来源和无匹配 ID 的旧样本保留，不按相近时间或颜色猜测删除。
- 最近 365 天、5,000 条上限不扩大，读取中 / 权限未知 / 失败 / 超出范围不误称确定无记录。进入后台或关闭连接后，所有健康派生视图随前台内存一起清空；回到前台重读，不创建持久化镜像。
- 覆盖升级保留 2.1.1 已开启的连接，不要求重新开启；仅尚未连接者明确开启并授权。读取用途说明覆盖按日期展示与独立健康回顾，文字笔记不写入健康，外部样本不反向写回。
- 上述标准字号小屏场景已点验；大字体、真实 VoiceOver、更多长来源及完整精度边界仍待补验，不概括为所有设备无溢出。颜色不是唯一编码，交互区域不能横向溢出。
- 所有界面夹具必须使用隔离合成样本，不读取真实健康数据；夹具与截图不进入生产 App 或日记存储。UI 夹具通过不能替代真实 iPhone 的授权、观察通知、覆盖升级和健康读写验收。

## 历史 2.0：全界面与网页核心流程

日期：2026-09-02。范围：现有 Expo 项目的全界面迭代与 GitHub Pages 网页发布。

结果：网页核心流程通过；原生端完成 iOS / Android JavaScript 与资源导出，未进行真机或商店审核验收。

### 参考与有意保留的差异

参考 [MoodKit 官方展示](https://moodkit.co/) 中 Today、Log、Drivers、Year 的界面，并在同一次图像观察中对照官方截图和本地手机截图。使用五档语义颜色、淡冷灰底、紫色操作色、白色圆角表面、三步弹层、四个主导航与悬浮记录按钮。

这是一版中文化改造，不是品牌或像素级复制。保留 MoodTracker 身份与原数据语义；不复制付费、冻结券、示例结论、官方 Logo。首页增加直接选心情入口；年像素采用可进入月份的十二个月布局；桌面为侧栏和双栏。无数据时保持真实空态，不植入营销演示记录。

图像对照证据在本次 Codex 任务的截图记录中。官方图只用于参考，没有作为产品资产提交。生产产物不包含验收样本。

### 视觉验收

| 项目       | 检查与结果                                                                                                                                        |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| 字体与层级 | 中文系统字体优先，标题 / 正文 / 辅助层级明确；320px 标题缩小，避免末尾孤字。长笔记、24 项活动摘要允许换行。                                       |
| 布局与间距 | 检查 320×740、390×844、768×1024、1280×720、1440×900；移动端单栏，平板居中的浮动导航，桌面侧栏。卡片与弹层边界无横向溢出。                         |
| 语义与色彩 | 提升浅色 secondary / muted / danger 对比；选中心情文字使用正文色。正文和操作标签以 4.5:1 为目标，必要图形以 3:1 为目标；禁用控件单独处理。        |
| 图标与资产 | MaterialCommunityIcons 统一图标族；独立生成应用图标和分享图；界面只加载 128px 品牌图，避免下载 1024px 图标。无装饰性手绘 SVG 或假插画。           |
| 边界与状态 | 空态、选中、禁用、悬停、焦点、保存中、错误、成功、删除确认、放弃修改均有表达。固定底栏不参与内容宽度计算。                                        |
| 可访问性   | 按钮 / 标签 / 输入有名称；装饰图标对读屏隐藏；弹层使用 dialog / aria-modal、焦点约束与返回，Escape 可关闭，修改内容先确认。主要操作至少 44px 高。 |
| 动态效果   | 支持减少动态效果；呼吸练习可暂停，后台暂停，完成后明确退出；Web 不请求原生动画驱动。                                                              |

### 已修复的问题

1. 手机年像素由百分比加固定 gap 导致七列折为六列：改为七等分外层、内边距间隔。
2. 24 项活动摘要撑开弹层：给文字容器宽度约束，并允许自然换行。
3. 320px 日期 / 时间输入默认最小宽度导致溢出：为输入设置 minWidth: 0。
4. 从活动列表翻页后保留滚动位置，遮住下一步标题：按记录步骤重置弹层滚动。
5. 平板底栏偏左、矮桌面侧栏底部拥挤：按可用宽度居中，按高度收起非关键装饰文案。
6. 选中色、辅助文字和按钮按压透明度降低可读性：调整 token，文字保持实色，用尺寸 / 背景反馈按压。
7. 多窗口保存、旧快照编辑 / 删除冲突：Web Locks 协调、串行队列、冲突时保留输入与新版本。
8. 跨分钟误报未保存：冻结初始日期 / 时间；非法未来日期与 DST 缺失小时明确拒绝。
9. 大体积中文备份在漂亮打印后超出导入上限：紧凑导出、真实 UTF-8 检查、预留 envelope 空间。
10. 活动关联只显示正差值而遗漏强负差值：改为绝对值排序，提供展开全部。
11. expo-font peer dependency 漂移到 SDK 57：显式锁定 SDK 54 兼容版本。
12. 固定文件名的离线资源可能永不更新：缓存版本加入全部资源内容 hash；不强制刷新未保存草稿。

### 实际交互验收

- 新建：选心情、活动多选 / 展开、选填笔记、补记日期 / 时间、快速保存。
- 编辑：修改心情和笔记后首页 / 日历 / 洞察同步；刷新后保留。
- 保护：未来日期拦截并保留输入；关闭未保存内容先确认；删除先确认，取消后记录保留。真正删除及失败回滚在内存测试中验证，不删除用户数据。
- 搜索：跨日期笔记搜索与心情筛选组合，找到目标记录。
- 统计：6 个本地验收样本得到全期日均 3.2、本周 4.3；工作活动两组各 3 天，差值 -2.3，与独立计算一致。没有把记录条数当成天数。
- 偏好：称呼和深色主题保存，重载后继续生效。
- 呼吸：开始、暂停、继续、60 秒完成、退出均验证。
- JSON：实际下载 6 条验收记录，再用正式 parser 验证完整往返。
- CSV：实际下载 7 条记录，验证 BOM、表头、行数。公式开头、换行、引号与 Unicode 由回归测试覆盖。
- 离线：停止本地生产服务器后刷新，应用、字体、图标与记录仍可读；离线新增一条，重载后仍存在。随后恢复服务器。
- 键盘：弹层打开聚焦关闭按钮，Shift+Tab 不能逃出弹层，Escape 返回原触发点。

### 自动化与发布检查

- TypeScript 检查通过。
- 数据层 62 项测试通过：旧 schema、失败持久化、重复提交、跨实例协调、旧快照冲突、日期 / DST、日均 / 关联、JSON / CSV、10 MB 边界。
- Expo Web 生产导出通过；资源使用 /moodtracker/ 前缀。
- 生产资源另有 19 项回归：预缓存文件存在性、manifest / 分享元信息、离线 / 503 回退、跨站点边界、缓存更新 hash 与不强制刷新策略。
- iOS / Android Hermes bundle 导出通过。它验证打包与模块引用，不等于已验证真机键盘、触觉或系统分享面板。
- GitHub Actions 在发布前重新运行检查；主分支通过后才部署 Pages。

### 明确限制

- 原生真机、VoiceOver / TalkBack、所有浏览器的系统文件选择器未逐台验收；本轮以网页核心流程和原生打包为交付边界。
- JSON 合并导入的校验、重复编号和失败原子性有自动测试；系统文件选择器的交互按 Expo 实现，不将单元测试称为真机验收。
- 21 项剩余上游依赖告警已记录于 [安全说明](docs/security.md)，没有声称零漏洞。
- 2.0 阶段默认不上传、不同步、不加密；后续版本的可选 Apple 健康自动同步不属于这份历史验收范围。当前存储与隐私边界见 [Apple 健康说明](docs/apple-health.md)，本产品不是医疗或诊断工具。
