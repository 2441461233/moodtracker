Remove the background from this image. Deliver an isolated logo cutout with a transparent background. Turn the white smiling-face strokes purple (#6C63DF) so the remaining cutout is a purple smiling face. Preserve the existing logo shape, location and stroke width. Clean anti-aliased edges. Background removal only, plus the white-to-purple recolor. Return the actual transparent cutout PNG file, not a preview of it.
