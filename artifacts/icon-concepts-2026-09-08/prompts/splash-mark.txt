Use case: background-extraction. Edit target: the supplied existing app icon, a white outlined rounded face on a flat purple square. Convert it into a transparent single-color launch-screen mark. Preserve the exact recognizable drawing: the softly squared outer loop, two circular eyes, rounded-ended smile, original proportions, stroke thickness, centering, and generous surrounding empty area. Convert the entire solid purple background into genuinely transparent pixels with alpha zero; recolor the former white face strokes and eyes to one solid purple #6C63DF so the mark is visible on a light app background. These are the only changes. Keep the 1:1 canvas and composition unchanged. No added tile, no white or colored background, no shadow, no checkerboard pixels, no new elements, no text, no crop. Return a real transparent-background PNG of the isolated purple mark, suitable for compositing over both light and dark launch-screen backgrounds.
