# 情绪像素图标提案

三个方案由内置 ImageGen 生成。用户最终确认方案 03「情绪像素」，并将产品名称定为「情绪像素」。已从获选原图通过 Expo 的图片处理流程导出并应用 1024 px 主图标、128 px 界面图标与 512 px PWA 图标；图案和构图保持获选版本。

| 方案 | 图像文件 | 最终提示词 |
| --- | --- | --- |
| 01 心情折页 | `01-folded-page.png` | `prompts/01-folded-page-v2.txt` |
| 02 温柔拥抱 | `02-gentle-embrace.png` | `prompts/02-gentle-embrace.txt` |
| 03 情绪像素 | `03-mood-pixels.png` | `prompts/03-mood-pixels-v2.txt` |

打开 `index.html` 可比较带圆角遮罩的大图及 48 px 小图，方案 03 已标为选定。

## 视觉检查

- 三张最终图均为 1254 × 1254 PNG，所有像素 alpha 为 255，适合作为后续 App 图标制作来源。
- 初版 01、03 的背景被错误去除且边缘出现噪点，已淘汰；重新生成后背景完整、轮廓清楚。
- 已在浏览器中检查大图、48 px 缩略图和浅深色桌面背景；无文字、截断及破损边缘。
- `assets/app-icon.png`、`assets/brand-icon.png`、`assets/pwa-icon.png` 已在用户确认后替换为方案 03 的对应尺寸；候选原图仍保留。
- 最终更名和图标应用后，重新通过 `npm run verify`；iOS prebuild 生成的主 App / 小组件名称为「情绪像素」/「情绪像素小组件」，1024 px 原生图标与获选素材的导出逐像素一致，且为无 alpha 通道的 RGB PNG。已在实际网页预览中确认新名称和新图标。

## 启动页处理状态

启动页采用与首页相同的浅深色纯色背景，暂不显示旧图标；启动控制等待数据及主题加载、首页或错误恢复页完成布局后再隐藏。这样当前项目可正常构建，也不会显示旧图标的底色方块。

内置图片工具此前的三次透明素材输出均未通过 alpha 检查：全部像素 alpha 为 255，棋盘格被画入图片。失败素材没有写入 App，当前启动页继续使用纯色衔接。

验证：TypeScript 检查、201 项应用测试、Web 导出和 19 项导出测试通过。隔离目录内的 iOS prebuild 成功，生成的 storyboard 不含图片视图或悬空约束，使用 `SplashScreenBackground` 命名颜色引用，浅深色资产均匹配首页背景。`plugins/with-plain-splash.js` 修正 SDK 54 无图片路径遗漏的背景引用及旧图片约束，之后若配置正式透明图片会自动跳过。原生冷启动效果仍须在新安装的正式 iOS 构建中验证，本次未提交新构建。
