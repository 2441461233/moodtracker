情绪像素 2.1.10（16）

修复键盘出现时文字输入框被遮挡的问题，保持输入框位于保存按钮上方，并调整收键盘按钮。

新增语音记录：记录第三步默认进入语音，支持暂停、继续、结束后保留本机原声、回听和单独导出；也可切换到文字补充。

AI 转写需要先在「我的空间 → 语音与 AI 转写 → 连接转写服务」配置可用的 HTTPS 服务和访问口令。本次仅完成 App 发布，转写后端的线上连接尚待配置，录音不会因服务未配置而丢失。

请在真机测试首次麦克风授权、暂停续录、结束录音、回听、保存后的再次回听与原声导出，并检查文字输入、键盘、切步和保存按钮。升级后确认原有记录、健康连接及大小两种组件正常。JSON 备份只包含文字，不含原声文件。
